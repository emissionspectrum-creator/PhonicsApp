# PhonicsApp — 自然發音學習工具

## 1. 專案目標

為一位 7 歲兒童設計的自然發音(phonics)練習工具。每天使用約 5 分鐘。

**使用者背景**:中文母語,英文聽力詞彙強(來自親子共讀與 Little Fox),缺的是「看到字母 → 喚起已知聲音」的解碼能力。會使用滑鼠。

**核心教學目標**:建立字素-音素對應(grapheme-phoneme correspondence),最終能自行讀出沒學過的字。

***

## 2. 根本設計決策

### 2.1 不做音素拼接

**放棄讓機器產生合成音。** 真人發音存在協同發音(coarticulation)——發 /k/ 時舌位已在為後續母音準備。獨立錄製的音素拼接後,無論間隔壓到多小,聽起來永遠是分離的音而非一個字。這是結構性限制,非工程精度問題。

**因此:所有音檔皆為完整單字的自然發音,程式永不做音檔拼接。**

合成(blending)這個認知動作發生在孩子的聽覺辨識與口語模仿中,不在程式裡。

### 2.2 錯誤即收穫

四個選項全部是**真詞**。題目播 `cat`,他選了 `h`,就播完整的 `hat` — 他沒有被懲罰,而是免費學到一個新字。

**推論(必須嚴格遵守):**

* 不顯示 X、不變灰、不移除錯誤選項
* 不播錯誤音效、不震動、不閃紅
* 錯誤選項在該題中維持完全可點(他可能想再聽一次 hat,那是好事)
* 「答對有動畫、答錯沒動畫」的不對稱本身就是最溫和的訊號

### 2.3 這是玩具,不是作業

無進度、無分數、無結束畫面。隨時能開、隨時能關。

***

## 3. 主 app(index.html)

### 3.1 版面:三帶

垂直分三帶,比例 **10% / 60% / 30%**。三帶邊界在整個 session 中**永不改變**。

```
┌─────────────────────────────────────┐
│ [-at]                               │  上帶 10%  控制列
├─────────────────────────────────────┤
│                                     │
│    ┌ ┄ ┐   ┌───┐┌───┐      ( ▶ )   │  中帶 60%  題目區
│    ┆   ┆   │ a ││ t │              │
│    └ ┄ ┘   └───┘└───┘              │
│                                     │
├─────────────────────────────────────┤
│     [b]   [c]   [h]   [m]           │  下帶 30%  選項區
└─────────────────────────────────────┘
```

**上帶** — 字族選單分階段、字族兩層,置於左上角。皆低對比、小尺寸,視覺上不邀請點擊(這是給大人用的)。選階段後,字族選單只列該階段的字族。

**中帶** — 視覺重心。

* 空槽:虛線外框、內部略暗。**不使用問號**(虛線傳達「拼圖缺一角」,問號傳達「考你」)
* 空槽與 `at` 之間留明顯間隙;`a`、`t` 兩格緊靠。此間隙在視覺上暗示 onset / rime 切分,不需任何文字說明
* 母音一律紅色,子音一律深色。此色彩慣例在後續混合母音階段會發揮作用
* 字母尺寸盡量大
* 重播鍵置於題目區右側,圓形喇叭圖示
* 翻頁鍵兩顆:「下一頁」置於重播鍵正下方;「上一頁」置於畫面左側、與「下一頁」等高的鏡像位置。左側對應重播鍵的位置留空

**下帶** — 選項磚橫排置中。

* 數量 3 或 4(見 3.4)
* **位置固定不洗牌**:同一字族練習期間,各字母固定在各自位置。位置穩定降低認知負擔,使注意力集中於聽音
* 滑鼠 hover 需有明顯回饋

**留白** — 帶與帶之間、選項磚之間留出大量空白。留白是將注意力鎖定於中帶的工具,不是浪費。

### 3.2 視覺規格

| 項目  | 規格                                                                                    |
| --- | ------------------------------------------------------------------------------------- |
| 背景  | 低飽和暖色(米白 / 淡奶油),不用純白或深色                                                               |
| 磚塊  | 實心色塊、圓角、輕微陰影                                                                          |
| 字體  | **必須使用單層 `a` 的字體**(Andika / Lexend / Century Gothic)。多數印刷體的 `a` 是雙層,與童書及手寫不一致,會造成真實困擾 |
| 大小寫 | 全部小寫                                                                                  |
| 視窗  | 假設全螢幕使用(Brave + F11)。設最小高度,低於則顯示提示訊息                                                  |

### 3.3 互動流程(一輪)

1. `[ ]at` 出現,自動播放完整單字錄音(如 `cat`)
2. 使用者點選一塊選項磚
3. 播放**他所選的那個字**的完整錄音(選 `h` → 播 `hat`)
4. **錯誤**:磚塊淡出、淡回原位。無標記、無音效。畫面停留,等待再次選擇
5. **正確**:磚塊進入空槽,三格合體成 `cat`(合體動畫與音檔播放**同時進行**,不留空白等待)
6. 播放完畢後,圖片由下往上滑入,佔據中帶
7. **單字保留在畫面上**,與圖片同時可見 — 這正是「這個字 \= 這個東西」的連結時刻
8. 點「下一頁」→ 下一個單字

**答對不播任何音效。** 圖片就是唯一回饋。

**翻頁**(取代原本的「點畫面任意處」):

* **下一頁** — 重播鍵正下方。**答對前淡化不可點**,答對後亮起。最後一題答對後維持淡化,這是「這一輪結束了」的唯一訊號,不做結束畫面
* **上一頁** — 畫面左側、與下一頁等高的鏡像位置。隨時可點(第一題時淡化)
* **回頭 \= 重新出題。** 按上一頁回到的是該字的未作答狀態:空槽、選項磚可點、自動播放題目音。不是回到已完成的圖片畫面。因此要再往前走,必須重新答對一次
* **每題的選項配置在該題第一次出現時即固定。** 前後翻動時,同一題的選項內容與位置永遠一致,不重抽干擾選項。翻回同一頁卻長得不一樣會造成困惑
* 按鈕上寫中文字「上一頁」「下一頁」。這是畫面上唯一的中文
* **點畫面空白處不再有任何作用**

**字義註記**(圖片出現後):

* 滑鼠移到圖片上**停留 0.4 秒**,圖片右上角浮現該字的中文字義。移開即消失
* **這是給大人看的。** 小尺寸、低對比,與上帶選單同一套視覺語言 — 視覺上不邀請注視。孩子不主動找就不會遇到
* 存在的理由是圖片有歧義:`tap` 是水龍頭不是輕拍、`tag` 是價格標籤不是鬼抓人。家長需要確認孩子看懂的是哪個意思
* **必須是 hover 而非常駐。** 常駐的中文會插進「字 \= 物」之間,把解碼變回翻譯,那正是 3.3 第 7 步要避免的
* 文字取自 `zh` 欄位。**不可改用 `note`** — `note` 是選字與畫面決策的紀錄,多數條目根本不含字義(如 `fin` 寫的是「需搭配魚的畫面才能清楚辨識部位」)

### 3.4 出題邏輯

* 字族內所有字**各出一次**,順序隨機
* 出完後停在最後一張圖上,**不自動循環**。要再玩就重選字族
* 干擾選項從同字族其他字中隨機抽取
* 選項數量:優先 4 個。字族真詞不足時縮為 3 個或 2 個,但**同一次 session 內數量必須一致**(不可這題 4 個下題 3 個)
* 系統不記錄任何作答結果(刻意排除),因此出題僅依隨機,不做適性調整

### 3.5 操作(全滑鼠)

| 動作        | 效果                     |
| --------- | ---------------------- |
| 點選項磚      | 選字                     |
| 點重播鍵      | 重聽題目。**次數無限、無任何提示或懲罰** |
| 點下一頁      | 下一個單字(答對後才可點)          |
| 點上一頁      | 上一個單字,回到其未作答狀態          |

不使用鍵盤。7 歲兒童尚未熟悉鍵盤字母位置,尋找按鍵會造成視線離開螢幕,切斷「聽到的音」與「看到的字母」的連結。

**細節:**

* 重播鍵在播放中被連點 → 中斷當前播放並重新播放
* 圖片顯示期間,選項磚鎖住不回應
* 音檔載入失敗 → **靜默略過,不跳錯誤對話框**,僅在 console 記錄。使用中跳出錯誤訊息極為干擾
* 預載下一題圖片(點選項當下即開始載入)

### 3.6 動畫

只做兩個:

| 時機 | 動畫                                                                         |
| -- | -------------------------------------------------------------------------- |
| 選對 | 磚塊進槽 + 三格合體。250–350ms,ease-out 結尾減速。抵達時槽邊框或磚塊輕微縮放一次(約 100ms),使其從「移動」變為「嵌合」 |
| 選錯 | 淡出、淡回原位。約 400ms。**不做飛行軌跡、不彈開、不震動**                                         |

**第一版可用退化實作**:選對時選項磚淡出、空槽內直接淡入該字母、三格收攏合體。純 opacity 與位置變化,不會出錯。「合體」是唯一具教學意義的動畫(它視覺化了 blending),飛行只是修飾,可後續再加。

實作注意:動畫結束瞬間應將磚塊的實際 DOM 位置替換,讓最終狀態由排版決定而非 transform 數值,避免對位偏差。

### 3.7 第一次使用

**不做任何 onboarding、教學動畫或說明文字。** 第一次由家長坐在旁邊示範一次即可。

***

## 4. 資料規格

### 4.1 words.json

**單一檔案**,以 `stage` 與 `family` 欄位分類。不拆分成多檔(混合對比題需跨字族查詢)。

| 欄位       | 說明                                |
| -------- | --------------------------------- |
| `word`   | 單字本身。同時作為音檔與圖片的檔名                 |
| `onset`  | 字首子音(如 `c`)                       |
| `rime`   | 韻腳(如 `at`)                        |
| `family` | 字族(如 `-at`)                       |
| `stage`  | 學習階段編號                            |
| `zh`     | 中文字義,顯示於畫面(見 3.3 字義註記)。單一詞,不含註記      |
| `note`   | 記錄選字與畫面決策(如 `tag = 價格標籤非鬼抓人`)。**不顯示於畫面**  |

**`onset` / `rime` 為後續混合對比題(pan/pin、hot/hat)預留。** 查詢方式為「onset 相同、rime 母音不同」。現在加入成本為零,日後補加需重寫整份字表。

**檔名由 `word` 推導**,不設額外欄位:

* 音檔 → `assets/audio/{word}.mp3`
* 圖片 → `assets/images/{word}.webp`

### 4.2 音檔

| 項目  | 規格                     |
| --- | ---------------------- |
| 來源  | edge-tts,**美式**(en-US) |
| 格式  | mp3(edge-tts 原生輸出,不轉檔) |
| 後處理 | **去頭尾靜音**、統一音量         |
| 內容  | 完整單字,永不拼接              |

**去頭尾靜音為必要步驟。** edge-tts 輸出前後常有數百毫秒空白,會破壞「我點了 → 我聽到」的即時因果感,而那正是整個互動的核心。

**生成後必須全部試聽一次。** TTS 偶有異常發音,混在正確檔案中不聽不會發現。

### 4.3 圖片

| 項目 | 規格                                     |
| -- | -------------------------------------- |
| 尺寸 | 1024×1024                              |
| 格式 | WebP                                   |
| 縮圖 | **不做**。WebP + 預載已足夠,日後若感受到延遲再補生成 512 版 |
| 背景 | 統一處理(統一白底或統一透明,**兩者擇一但必須一致**)          |
| 來源 | AI 生圖(非圖庫)                             |

**AI 生圖為主要來源,非備案。** 除了能處理 wag、nap 這類難以尋找的字,更重要的是可用同一段風格描述貫穿全部圖片,達成圖庫做不到的風格一致性。

風格描述與提示詞產製屬於 Claude Project instruction 的範圍,**不在本文件內**。程式僅約束格式與命名。

每張圖仍需人眼確認(AI 可能將 `tap` 生成為「輕點手指」而非水龍頭)。

***

## 5. admin.html

### 5.1 定位

**開發期工作台**,非後台系統。與 `index.html` 完全分離,孩子看不到任何管理功能。

第一階段即建置(開發期需頻繁試 voice、聽發音、替換圖片,需要快速介面)。

### 5.2 範圍(僅三項)

1. **音檔試聽 + 重生成** — 逐字播放鍵;可換 voice 或調整 rate 後重新生成
2. **圖片拖放** — 顯示缺圖清單,拖入後自動改名、轉 WebP、統一背景處理。消除檔名打錯這個錯誤來源。**檔名重複時直接覆蓋**(拖入的意圖就是替換)
3. **缺漏檢查** — 直接顯示於頁面,不需切換到終端機

### 5.3 明確不做

字表編輯表單、字族管理、批次操作、統計報表。

字表交由 Claude Code 直接編輯 JSON 更快,且做了表單反而多一個需要同步的東西。

***

## 6. 專案結構與執行

### 6.1 目錄

```
/mnt/data/PhonicsApp/
├── index.html          主 app
├── admin.html          維護頁面
├── server.py           Flask(靜態服務 + 上傳端點)
├── words.json          字表
├── assets/
│   ├── audio/          {word}.mp3
│   └── images/         {word}.webp
├── scripts/
│   ├── generate_audio.py   只補缺少的音檔,不重生成已有的
│   └── check_assets.py     缺漏檢查
├── start.sh            Ubuntu 啟動
└── start.bat           Windows 啟動
```

### 6.2 執行方式

**必須透過 local server,不可用 `file://` 直接開啟。** 瀏覽器對本機檔案有嚴格限制,`fetch('words.json')` 會被 CORS 擋下。所有瀏覽器皆然,非 Brave 特有。

由**單一 Flask** 同時服務 `index.html` 與 `admin.html`。admin 的圖片拖放需要寫檔能力,超出靜態 server 範圍,既然需要 Flask,就不再另外維護靜態 server。

**Flask 在此僅為檔案伺服器加一個上傳端點 — 無資料庫、無狀態、無業務邏輯。**

`start.sh` / `start.bat` 雙擊即啟動 server 並開啟瀏覽器,使用時不需開終端機。

### 6.3 跨 OS 與路徑

* 程式碼放在 Linux 原生檔案系統(`~/Dev/PhonicsApp/`),由私有
  GitHub repo 同步。**不在 NTFS 上開發** — NTFS 缺乏 POSIX
  權限,執行位元與 git 檔案模式會產生問題
* assets(音檔、圖片)存放於共用 NTFS 磁碟,兩系統共用同一份
* **assets 路徑支援環境變數 `ASSETS_DIR` 覆寫,預設值 `./assets`**
  * Ubuntu:`~/Dev/PhonicsApp/assets` 為 symlink,指向
    `/mnt/data/PhonicsApp/assets`。使用預設值即可,symlink
    加入 .gitignore
  * Windows:由 `start.bat` 設定 `ASSETS_DIR=D:\PhonicsApp\assets`
* 除 assets 外,程式不可寫死任何絕對路徑
* **Windows Fast Startup 必須保持關閉**,否則共用 NTFS 磁碟有
  損毀風險
* assets 產製成本高(尤其圖片),建議階段性完成時另行備份

### 6.4 檢查腳本輸出格式

`check_assets.py` 是使用者與 Claude Code 之間的交接介面。

**缺圖片的項目必須單獨列成一區、可直接複製的清單**,不可與其他檔案路徑混雜。找圖是唯一無法自動化的步驟,清單格式直接影響工作效率。

***

## 7. 學習順序

### 7.1 核心原則:先橫向,後縱向

**橫向** \= 同一母音下擴充多個 rime(-at → -an → -ap → -ag)
**縱向** \= 更換母音(a → i → o)

**必須先做橫向。** 母音是最難的部分——短母音 a/e/i/o/u 差異細微,且中文無對應。同時只處理一個母音,孩子才能專心對付子音變化;若一開始就 -at 跳 -ig 跳 -op,每次同時變動兩個變數,規則無法浮現。

### 7.2 階段

| 階段 | 字族                 | 範例字                                                                          |
| -- | ------------------ | ---------------------------------------------------------------------------- |
| 一  | -at, -an, -ap, -ag | cat, bat, hat, mat / can, man, pan, fan / cap, map, nap, tap / bag, tag, rag |
| 二  | -ig, -in, -ip, -it | pig, big, dig, wig / pin, win, bin, fin / lip, tip, zip / sit, hit, bit      |
| 三  | -op, -ot, -ox      | top, mop, hop, pop / hot, pot, dot / box, fox                                |
| 四  | -ug, -un, -ut, -up | bug, rug, hug, mug / sun, run, fun, bun / cut, nut, hut / cup, pup           |
| 五  | -et, -en, -ed      | net, pet, wet, jet / pen, hen, ten / bed, red, fed                           |
| 六  | 五母音總混合             | hat / hit / hot / hut 四選一                                                    |

**階段六為程式推導,`stage: 6` 保留、不寫入 words.json。** 對比組由「onset 相同、尾子音相同」自動歸組(同組 ≥3 字才出現),實作方式與現有組清單見 [HANDOFF.md](./HANDOFF.md)。

**階段一應佔掉總時間的一半。** 此階段要焊死的不是那二十個字,而是「a \= /æ/」這個基準,後續所有母音都以此為參照。

**`-og` 延後。** 美式 dog 的母音(/ɔ/)與 hot、pot 的短 o 不同,不適合作為教母音規則的範例。dog 太常用不學可惜,但等 o 穩固後再收。

### 7.3 混合對比題

**每階段後半必須做混合對比,那才是真正的訓練。** 在 -at 內把 c 換成 h,實際只在聽開頭子音,難度不高。真正困難也真正有用的是只差一個母音的對決:

* 階段二:pan / pin、bag / big
* 階段三:hot / hat、top / tap
* 階段四:cup / cap、bug / bag、run / ran
* 階段五:pen / pan、bed / bad、ten / tan

此功能依賴 `onset` / `rime` 欄位查詢,已實作為階段六;推導規則見 [HANDOFF.md](./HANDOFF.md)。

### 7.4 CVC 之後

階段七:字尾雙子音(-ck, -ll, -ss, -ng)→ 階段八:開頭子音串(st-, fl-, gr-)→ 階段九:子音 digraph(sh, ch, th, wh)→ 階段十:magic e(cap/cape、kit/kite)→ 階段十一:母音組合(ee, ea, oo, ai, oa)

原則同樣是一次只動一個變數:CVC 之後先擴充子音複雜度,母音維持不變,待五個短母音穩固後才進入長母音。

sight words(the, is, of, was, said)不遵守發音規則,不適用本 app 的機制,應於共讀時另行處理。

### 7.5 驗證方式

每階段結束時,給一個**從未出現在練習中的字**(如 -ag 練完後給 `sag`),看能否自行讀出。

讀得出來代表規則已內化,可以前進;讀不出來代表只是背了那些字,需回頭多練。

**此驗證由家長口頭進行,不做進 app。**

***

## 8. 加字流程

| 步驟                    | 執行者                    |
| --------------------- | ---------------------- |
| 列出候選字 + 中文意思 + 是否具體   | Claude Project(字彙產製專用) |
| **篩選**                | **家長**                 |
| 寫入 words.json         | Claude Code            |
| 生成音檔(只補缺的)            | Claude Code            |
| 生成圖片提示詞               | Claude Project         |
| 生圖(ChatGPT API)+ 人眼確認 | **家長**                 |
| 圖片拖入                  | 家長(透過 admin)           |
| 缺漏檢查                  | Claude Code / admin    |

**分工原則**:判斷性工作(選字、順序、風格)在 Claude Project 討論;執行性工作(寫檔、跑腳本)交給 Claude Code。

Claude Code 指令中應明確加入「不要自行尋找或產生圖片」,否則可能生出不可用的檔案。

***

## 9. 明確不做的事

### 功能面

* 語音辨識評分(兒童口音辨識率低,誤判直接打擊信心)
* 進度追蹤、分數、星星、排行榜、獎章
* 計時器、題號顯示、進度條
* 結束畫面或「恭喜完成」慶祝頁
* 帳號、雲端同步、多使用者
* 錯題記錄與適性出題
* 音素拼接播放(見 2.1)
* 鍵盤操作
* 無聲模式(原 C 方案,已取消 — 家長一律先用播放鍵,再視情況跟著唸)
* 響應式設計(固定桌機全螢幕使用)
* onboarding 教學流程

### 為什麼

有進度條他就會想快點結束。有分數他就會在意分數而非字。這個 app 的價值有一半來自畫面上**沒有**的東西。

***

## 10. 開發順序

1. `server.py` + `admin.html` — 先備妥 -at 字族的音檔與圖片
2. `index.html` — 僅支援 -at 字族,動畫用退化版本
3. 實際使用一週,驗證設計有效性
4. 補上飛行動畫、擴充至階段一其餘字族
5. 階段二起加入混合對比題

**先驗證再擴充。** 若實際體驗不如預期,設計需調整的成本遠低於已建置大量素材後才發現。